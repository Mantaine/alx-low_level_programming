#include "main.h"

/**
 * _memcpy - copies memory area
 * @dest: where stored
 * @src: where copied
 * @n: number of bytes
 * Return: copied memory within n bytes changed
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
	int r = 0;
	int h = n

		for (; r < h; r++)
		{
			dest[r] = src[r];
			n--;
		}
	return (dedt);
}
